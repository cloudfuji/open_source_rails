!!!
%html
  %head
    = stylesheet_link_tag 'application', :media => 'screen, projection'
    = javascript_include_tag 'application'
    = csrf_meta_tags

  %body
    .header_bg
    .wrapper
      %header
        .site_meta
          OpenSourceRails
        .nav
          = link_to "Submit", new_project_path
          -if user_signed_in?
            = link_to "Sign out", destroy_user_session_path
          -else
            = link_to "Sign in", new_user_session_path
            = link_to "Sign up", "http://bushi.do/users/sign_up"

      - unless notice.nil?
        %p{:class=>"notice"}
          = notice
      - unless alert.nil?
        %p{:class=>"alert"}
          = alert
      
      = yield
    %footer
      .fwrap
        Powered by Bushido | Special thanks to @railsjedi
